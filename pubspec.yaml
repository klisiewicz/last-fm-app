name: last_fm_app
description: A LastFM app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  auto_route: ^2.1.0
  dio: ^4.0.0
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  flutter_hooks: ^0.17.0
  hooks_riverpod: ^0.14.0+4
  meta: ^1.3.0
  transparent_image: ^2.0.0

dev_dependencies:
  auto_route_generator: ^2.1.0
  build_runner: ^2.1.1
  flutter_test:
    sdk: flutter
  lint: ^1.6.0
  mocktail: ^0.1.4
flutter:
  uses-material-design: true
  assets:
    - .env
